import Credentials from "@/components/Navigation/Login/Credentials";
import LoginLayout from "@/layouts/login";

export default function CredentialsPage() {

    return (
        <LoginLayout>
            <Credentials />
        </LoginLayout>
    )
}